const assert = require("node:assert/strict");

class Solution {
  solve(value) {
    // Replace this method with the LeetCode solution.
    return value;
  }
}

const solution = new Solution();

assert.equal(solution.solve(1), 1);
assert.equal(solution.solve(-1), -1);

console.log("All tests passed.");

